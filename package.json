{
  "name": "new-folder-yes",
  "version": "1.0.0",
  "description": "Same as the last assignment but you need to use jwts for authentication.\r We have introduced the signgin endpoints for both users and admins.\r For this one, in every authenticated requests, you need to send the jwt in headers (Authorization : \"Bearer <actual token>\").\r You need to use mongodb to store all the data persistently.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.3",
    "nodemon": "^3.0.2",
    "zod": "^3.22.4"
  }
}
